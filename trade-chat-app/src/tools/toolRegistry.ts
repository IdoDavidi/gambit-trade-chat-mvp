import { setTeams } from "./setTeams";
import { addPlayer } from "./addPlayer";
import { requestVerdict } from "./requestVerdict";

export const toolRegistry = {
    setTeams,
    addPlayer,
    requestVerdict,
};


